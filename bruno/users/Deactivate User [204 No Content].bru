meta {
  name: Deactivate User [204 No Content]
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/api/users/{{userId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
