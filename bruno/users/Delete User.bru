meta {
  name: Delete User
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/users/{{createdUserId}}
  body: none
  auth: none
}

script:post-response {
  bru.setEnvVar("createdUserId", undefined);
}
