meta {
  name: Delete User
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/api/users/{{createdUserId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

script:post-response {
  bru.setEnvVar("createdUserId", undefined);
}
