meta {
  name: Change User Role
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/api/users/{{createdUserId}}/role
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{adminAccessToken}}
}

body:json {
  {
    "role": "ROLE_USER"
  }
}
