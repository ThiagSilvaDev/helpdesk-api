meta {
  name: Mark Read [200 OK]
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/api/notifications/{{notificationId}}/read
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
