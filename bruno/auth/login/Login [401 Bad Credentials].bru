meta {
  name: Login [401 Bad Credentials]
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "{{login_email}}",
    "password": "Wrong@123456"
  }
}

settings {
  encodeUrl: false
  followRedirects: true
  maxRedirects: 5
  timeout: inherit
}
