meta {
  name: Get User Profile
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/auth/me
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{userAccessToken}}
}

script:post-response {
  if (res.status === 200 && res.body?.id) {
    bru.setEnvVar("devUserId", res.body.id);
  }
}
