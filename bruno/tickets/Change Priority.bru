meta {
  name: Change Priority
  type: http
  seq: 8
}

patch {
  url: {{baseUrl}}/api/tickets/{{createdTicketId}}/priority
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

body:json {
  {
    "priority": "HIGH"
  }
}

