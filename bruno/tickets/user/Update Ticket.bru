meta {
  name: Update Ticket
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/api/users/tickets/{{createdTicketId}}
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{userAccessToken}}
}

body:json {
  {
    "title": "Server is down",
    "description": "The main server is not responding since 10am. All services are affected."
  }
}

