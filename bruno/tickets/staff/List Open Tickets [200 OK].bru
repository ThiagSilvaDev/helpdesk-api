meta {
  name: List Open Tickets [200 OK]
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/api/staff/tickets
  body: none
  auth: bearer
}

params:query {
  status: OPEN
  page: 0
  size: 20
  sort: createdAt,desc
}

auth:bearer {
  token: {{accessToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
