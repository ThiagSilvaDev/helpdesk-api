meta {
  name: Assign Technician [400 Invalid Field Type]
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/api/staff/tickets/{{ticketId}}/technician
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "technicianId": "not-a-number"
  }
}
