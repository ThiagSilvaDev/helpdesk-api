meta {
  name: Close Ticket [204 No Content]
  type: http
  seq: 6
}

patch {
  url: {{baseUrl}}/api/staff/tickets/{{ticketId}}/close
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
