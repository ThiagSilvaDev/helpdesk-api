meta {
  name: Get All Open Tickets
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/api/tickets
  body: none
  auth: inherit
}

params:query {
  status: OPEN
  page: 0
  size: 20
  sort: createdAt,desc
}

headers {
  Authorization: Bearer {{accessToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
